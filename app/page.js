export default function Home() {
    let name = "chan";

    return (
        <div>
            <h4 className="title">chan fresh</h4>
            <p className="title-sub">by dev {name}</p>
        </div>
    );
}
